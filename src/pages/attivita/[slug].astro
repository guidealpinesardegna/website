---
import ActivityPage from "../../features/activity/ActivityPage.astro";
import { activitiesStaticPaths } from "../../lib/staticPaths";

export function getStaticPaths() {
  return activitiesStaticPaths("it");
}

const { activity } = Astro.props;
---

<ActivityPage lang="it" activity={activity} activeMenuLink="attivita" />
